import React from 'react'
import Navbar from '../components/navbar'
const Layout = ({ children }) => {
  return <div style={{ height: '100vh', paddingTop: 40 }}>{children}</div>
}

export default Layout
